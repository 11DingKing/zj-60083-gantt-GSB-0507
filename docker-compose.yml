version: '3.8'

services:
  web:
    build: .
    ports:
      - "16083:5173"
    volumes:
      - ./src:/app/src
      - ./index.html:/app/index.html
      - ./vite.config.ts:/app/vite.config.ts
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: npm run dev
